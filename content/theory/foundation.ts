import type { TheorySections } from '@/content/theory/helpers';
import { code, list, note, paragraph, table } from '@/content/theory/helpers';

const checkpoint = (
  explanation: string,
  ...steps: string[]
): TheorySections[number] => ({
  title: 'Перед переходом до задачі',
  blocks: [paragraph(explanation), list(...steps)],
});

export const foundationTheoryAdditions: Record<string, TheorySections> = {
  'direct-implementation': [
    {
      title: 'Від тексту умови до безпомилкової програми',
      blocks: [
        paragraph(
          'У задачі на пряму реалізацію алгоритм часто вже описано словами. Складність полягає в точному перекладі: кожен об’єкт умови має стати змінною або контейнером, кожне правило — гілкою чи формулою, а порядок дій у коді має збігатися з порядком у специфікації. Перед програмуванням корисно скласти таблицю «фраза умови → дія програми» та окремо виписати граничні випадки.',
        ),
        table(
          ['Фраза в умові', 'Що з’являється у коді'],
          [
            ['«для кожного елемента»', 'цикл for або range-for'],
            ['«якщо… інакше…»', 'if / else if / else'],
            ['«не перевищує 10^18»', 'long long, а не int'],
            ['«вивести з точністю»', 'fixed і setprecision'],
          ],
        ),
        code(
          `long long answer = 0;
for (int i = 0; i < n; ++i) {
    long long x;
    cin >> x;
    if (x > 0) answer += x;
}
cout << answer << '\\n';`,
          'Читання, перевірка правила і накопичення відповіді',
        ),
        note(
          'Перевірка перед submit',
          'Зістав код з умовою рядок за рядком. Перевір мінімальний input, максимальні значення, рівність на межі та формат виводу. У implementation-задачах саме ці деталі частіше дають WA, ніж вибір алгоритму.',
        ),
      ],
    },
    checkpoint(
      'Для прямої реалізації корисно провести сухий запуск: вручну записати значення ключових змінних після кожної ітерації на маленькому input. Так видно, чи цикл обробляє рівно потрібні елементи, а умови на межі збігаються з математичним формулюванням.',
      'Назви тип і допустимий діапазон кожної змінної.',
      'Перетвори кожен пункт умови на конкретну дію коду.',
      'Перевір рівність на всіх межах: < проти <=.',
      'Зістав пробіли, переноси рядків і порядок значень у виводі.',
    ),
  ],
  'process-simulation': [
    {
      title: 'Стан, подія і перехід',
      blocks: [
        paragraph(
          'Симуляцію зручно мислити як автомат. Стан — це мінімальний набір даних, який повністю описує поточний момент. Подія або один крок читає цей стан і створює наступний. Якщо для продовження потрібна інформація, якої у стані немає, модель неповна; якщо змінна ніколи не впливає на майбутнє чи відповідь, вона зайва.',
        ),
        list(
          'Визнач початковий стан до першої події.',
          'Для однієї події письмово зафіксуй порядок оновлень.',
          'Після кожного кроку сформулюй інваріант: що саме вже оброблено.',
          'Виріши, чи потрібен цикл за кожну секунду, чи можна переходити одразу між важливими подіями.',
        ),
        code(
          `int position = 0;
for (char command : commands) {
    if (command == 'L') --position;
    else if (command == 'R') ++position;
}
cout << position << '\\n';`,
          'Послідовність команд як симуляція переходів',
        ),
        note(
          'Старе чи нове значення?',
          'Коли кілька величин змінюються одночасно, не перезаписуй одну з них до того, як використав її старе значення. Обчисли nextState в окремих змінних, а потім присвой увесь новий стан.',
        ),
      ],
    },
    checkpoint(
      'Перед великою симуляцією відокрем стан від історії. Зберігати всі попередні конфігурації потрібно лише тоді, коли до них звертається майбутнє або треба знайти цикл. Інакше достатньо поточного стану та відповіді, що накопичується.',
      'Випиши початковий стан до першої команди.',
      'Уточни, чи події відбуваються послідовно або одночасно.',
      'Знайди верхню межу кількості кроків, щоб оцінити час.',
      'Перевір команду, яка нічого не змінює, та останню подію.',
    ),
  ],
  'frequency-counting': [
    {
      title: 'Частоти як стиснення повторів',
      blocks: [
        paragraph(
          'Масив частот замінює довгу послідовність відповіддю на питання «скільки разів зустрічається кожне значення?». Це корисно, коли порядок елементів неважливий. Вибір структури залежить від області значень: для щільного невеликого діапазону найпростіший vector, для великих або рядкових ключів — map чи unordered_map.',
        ),
        table(
          ['Ключі', 'Структура', 'Типова вартість'],
          [
            ['0…m, m помірне', 'vector<int> freq(m + 1)', 'O(1)'],
            ['великі, потрібен порядок', 'map<Key, int>', 'O(log k)'],
            ['великі, порядок не потрібен', 'unordered_map<Key, int>', 'O(1) у середньому'],
          ],
        ),
        code(
          `unordered_map<string, int> freq;
for (const string& word : words) {
    ++freq[word];
}

for (const auto& [word, count] : freq) {
    if (count >= 2) cout << word << '\\n';
}`,
          'Підрахунок повторюваних рядків',
        ),
        note(
          'Що втрачаємо',
          'Частоти не пам’ятають позиції. Якщо відповідь залежить від першої появи, сусідства або порядку, додай цю інформацію окремо чи обери іншу модель.',
        ),
      ],
    },
    checkpoint(
      'Після побудови частот відповідь часто обчислюється вже не за n елементами, а за k різними ключами. Це може спростити формулу, але пам’ятай: ітерація по unordered_map має довільний порядок, тоді як map повертає ключі відсортовано.',
      'Визнач повний діапазон ключів і можливу кількість різних значень.',
      'Обери тип лічильника: сума частот або число пар може вимагати long long.',
      'Виріши, чи нульова частота повинна залишатися у структурі.',
      'Перевір порожню послідовність і випадок, де всі значення однакові.',
    ),
  ],
  'sorting-to-simplify': [
    {
      title: 'Що дає і що забирає сортування',
      blocks: [
        paragraph(
          'Сортування створює монотонний порядок: близькі за значенням елементи стають сусідами, а всі менші розміщуються перед більшими. Завдяки цьому перебір усіх пар часто замінюється переглядом сусідніх елементів, binary search або two pointers. Але початковий порядок зникає, тому індекси потрібно зберегти заздалегідь, якщо вони є частиною відповіді.',
        ),
        code(
          `vector<pair<int, int>> items;
for (int i = 0; i < n; ++i) {
    items.push_back({a[i], i});
}
sort(items.begin(), items.end());
// items[k].first  — значення
// items[k].second — початковий індекс`,
          'Сортування зі збереженням початкових позицій',
        ),
        list(
          'Сформулюй, яка властивість стане локальною після сортування.',
          'Доведи, чому достатньо перевірити сусідів або рухати межу лише в один бік.',
          'Порахуй повну складність: sort дає O(n log n), навіть якщо подальший прохід лінійний.',
        ),
        note(
          'Сортування не є доказом',
          'Фраза «відсортуємо, а далі очевидно» приховує головну частину розв’язання. Треба пояснити, чому жодна краща відповідь не загубилась через новий порядок.',
        ),
      ],
    },
    checkpoint(
      'Після сортування зроби маленький доказ «чому локально». Наприклад, якщо шукаємо мінімальну різницю, між двома несусідніми значеннями лежить сусідня пара з не більшою різницею. Саме таке твердження скорочує кандидатів, а не сам виклик sort.',
      'Назви властивість, яку зберігає перестановка елементів.',
      'Назви інформацію, яку сортування руйнує, і чи потрібна вона у відповіді.',
      'Випиши, які кандидати залишаться після впорядкування.',
      'Перевір дублікати та вже відсортований масив.',
    ),
  ],
  'custom-sort-order': [
    {
      title: 'Коректний comparator і правила нічиєї',
      blocks: [
        paragraph(
          'Comparator відповідає на питання «чи повинен a стояти строго перед b?». Він має задавати строгий порядок: comp(a, a) завжди false, а суперечливі цикли на кшталт a < b < c < a заборонені. Для рівних основних ключів додають tie-breaker, інакше порядок таких елементів не визначений.',
        ),
        code(
          `struct Student {
    string name;
    int solved;
    int penalty;
};

sort(students.begin(), students.end(), [](const Student& a, const Student& b) {
    if (a.solved != b.solved) return a.solved > b.solved;
    if (a.penalty != b.penalty) return a.penalty < b.penalty;
    return a.name < b.name;
});`,
          'Кілька ключів: задачі ↓, штраф ↑, ім’я ↑',
        ),
        table(
          ['Потреба', 'Інструмент'],
          [
            ['власний порядок', 'sort + comparator'],
            ['зберегти порядок рівних', 'stable_sort'],
            ['лексикографічні ключі', 'pair / tuple'],
          ],
        ),
        note(
          'Типова помилка',
          'Не використовуй <= або >= в comparator: для рівних елементів це поверне true і порушить контракт sort. Порівняння повинно бути строгим.',
        ),
      ],
    },
    checkpoint(
      'Для складного comparator склади кілька пар об’єктів і вручну перевір comp(a, b), comp(b, a) та comp(a, a). Якщо основні ключі рівні, правило нічиєї повинно бути явним або доведено неважливим для результату.',
      'Запиши пріоритети ключів у порядку порівняння.',
      'Для кожного ключа познач напрям: ↑ чи ↓.',
      'Не захоплюй у lambda змінні, які змінюються під час sort.',
      'Перевір однакові записи та крайні значення ключів.',
    ),
  ],
  'basic-stl-structures': [
    {
      title: 'Обирай контейнер від потрібних операцій',
      blocks: [
        paragraph(
          'Назва структури менш важлива за набір операцій. Спочатку випиши, що програма робитиме найчастіше: доступ за індексом, перевірка наявності, key→value чи отримання екстремуму. Потім звір асимптотику. Контейнер зручний лише тоді, коли його типові операції відповідають моделі задачі.',
        ),
        table(
          ['Потрібна операція', 'Типовий вибір', 'Зауваження'],
          [
            ['індексований доступ', 'vector', 'O(1), елементи зберігають порядок'],
            ['key → value', 'map / unordered_map', 'порядок проти середнього O(1)'],
            ['унікальність та ordered lookup', 'set', 'O(log n)'],
            ['постійний min/max', 'priority_queue', 'верхівка O(1), зміни O(log n)'],
          ],
        ),
        code(
          `priority_queue<int, vector<int>, greater<int>> minimums;
minimums.push(7);
minimums.push(3);
minimums.push(5);
cout << minimums.top() << '\\n'; // 3`,
          'Мінімальна priority_queue',
        ),
        note(
          'Не змішуй інтерфейси',
          'У priority_queue немає довільного доступу чи пошуку, а map не є масивом. Якщо задача потребує операції, якої структура не підтримує, це сигнал переглянути вибір, а не намагатися обійти інтерфейс дорогим циклом.',
        ),
      ],
    },
    checkpoint(
      'Окремо враховуй життєвий цикл iterator і reference. push_back може перевиділити vector та зробити старі посилання невалідними; erase змінює ітератори; top/front не можна викликати для порожньої структури. Це не алгоритмічні властивості, але вони часто спричиняють RE.',
      'Склади список операцій та їх кількість у найгіршому випадку.',
      'Перевір, чи потрібен порядок ключів або лише наявність.',
      'Не використовуй map[key] для простої перевірки, якщо не хочеш вставляти ключ.',
      'За потреби зберігай пари {priority, id}, щоб розрізняти рівні елементи.',
    ),
  ],
  'prefix-sum': [
    {
      title: 'Півінтервали, типи та варіанти prefix',
      blocks: [
        paragraph(
          'Найстійкіший шаблон використовує pref[0] = 0 і pref[i + 1] як суму перших i + 1 елементів. Тоді сума півінтервалу [l, r) дорівнює pref[r] − pref[l]. Єдина формула працює і для порожнього відрізка, і для l = 0, тому в коді менше окремих випадків.',
        ),
        code(
          `vector<long long> pref(n + 1, 0);
for (int i = 0; i < n; ++i) {
    pref[i + 1] = pref[i] + a[i];
}

auto rangeSum = [&](int l, int r) { // [l, r)
    return pref[r] - pref[l];
};`,
          'Prefix Sum із півінтервалами [l, r)',
        ),
        paragraph(
          'Так само будують prefix count для умови isGood(a[i]), prefix minimum/maximum у спеціальних задачах або кілька масивів префіксів для різних категорій. Для сум майже завжди варто використовувати long long: навіть якщо кожне a[i] міститься в int, сума n елементів може не міститися.',
        ),
        note(
          'Prefix працює на незмінних даних',
          'Після зміни a[i] треба перерахувати всі наступні pref. Якщо оновлень багато, згодом знадобляться Fenwick або Segment Tree; це preview, знати їх реалізацію на Foundation ще не потрібно.',
        ),
      ],
    },
    checkpoint(
      'Prefix — це відповідь для всіх початкових відрізків. Довільний відрізок отримуємо як різницю двох історій: інформація до r мінус інформація до l. Підхід узагальнюється лише тоді, коли агрегат допускає таке вилучення або має спеціальну формулу.',
      'Вибери одну конвенцію відрізків і підпиши її біля формули.',
      'Порахуй максимальну можливу суму до вибору типу.',
      'Відокрем preprocessing O(n) від кожного query O(1).',
      'Перевір [0, n), один елемент і порожній півінтервал.',
    ),
  ],
  'difference-array': [
    {
      title: 'Зміни на межах замість кожної позиції',
      blocks: [
        paragraph(
          'Difference Array зберігає не значення, а моменти, де активна добавка змінюється. Для додавання x на [l, r] ми запускаємо ефект у l через diff[l] += x і зупиняємо після r через diff[r + 1] -= x. Один prefix-прохід відновлює сумарну добавку в кожній позиції.',
        ),
        code(
          `vector<long long> diff(n + 1, 0);
for (auto [l, r, x] : queries) {
    diff[l] += x;
    if (r + 1 < n) diff[r + 1] -= x;
}

long long add = 0;
for (int i = 0; i < n; ++i) {
    add += diff[i];
    a[i] += add;
}`,
          'Багато inclusive-оновлень [l, r], один фінальний прохід',
        ),
        list(
          'Узгодь 0-based чи 1-based індексацію до написання формули.',
          'Виділи n + 1 елемент або окремо перевіряй r + 1.',
          'Пам’ятай: метод дає фінальний масив після всіх офлайн-оновлень, а не миттєві відповіді між ними.',
        ),
        note(
          'Модель подій',
          'diff[l] — подія «добавка почалась», diff[r + 1] — «добавка завершилась». Це пояснення надійніше за механічне запам’ятовування двох формул.',
        ),
      ],
    },
    checkpoint(
      'Difference Array особливо добре пояснюється через похідну: всередині однакової добавки різниця не змінюється, а на межах виникають стрибки. Prefix Sum виконує зворотну операцію та інтегрує ці стрибки у значення.',
      'Визнач, чи оновлення inclusive або half-open.',
      'Перевір праву межу масиву та доступ до r + 1.',
      'Виконай усі updates до фінального відновлення.',
      'Оціни максимальну сумарну добавку в одній позиції.',
    ),
  ],
  'prefix-xor-2d': [
    {
      title: 'Inclusion–exclusion на прямокутнику',
      blocks: [
        paragraph(
          'Двовимірний prefix pref[i][j] зберігає суму прямокутника від початку таблиці до позиції перед (i, j), якщо використовувати півінтервали. Щоб знайти довільний прямокутник, беремо великий prefix, віднімаємо зайві смуги зверху і зліва та повертаємо кут, який відняли двічі. Це звичайний принцип inclusion–exclusion.',
        ),
        code(
          `vector<vector<long long>> pref(n + 1, vector<long long>(m + 1));
for (int i = 0; i < n; ++i) {
    for (int j = 0; j < m; ++j) {
        pref[i + 1][j + 1] = a[i][j] + pref[i][j + 1]
                            + pref[i + 1][j] - pref[i][j];
    }
}

// сума [r1, r2) × [c1, c2)
long long sum = pref[r2][c2] - pref[r1][c2]
              - pref[r2][c1] + pref[r1][c1];`,
          '2D Prefix із нульовою рамкою',
        ),
        paragraph(
          'Нульовий рядок і нульовий стовпець прибирають перевірки меж. Ціна — пам’ять O(nm), тому завжди оцінюй її: таблиця 5000×5000 з long long займає приблизно 200 MB лише для prefix.',
        ),
        note(
          'Prefix і suffix',
          'Prefix накопичує інформацію ліворуч або вище від межі; suffix — праворуч або нижче. Це та сама ідея preprocessing, лише напрям проходу інший.',
        ),
      ],
    },
    checkpoint(
      'Намалюй потрібний прямокутник і чотири prefix-прямокутники різними кольорами. Знак кожного члена тоді випливає з кількості покриттів: потрібна область має залишитися один раз, смуги — зникнути, а спільний кут — повернутися після подвійного віднімання.',
      'Зафіксуй порядок координат: row/column або x/y.',
      'Використай додаткову нульову рамку, щоб прибрати гілки.',
      'Перевір прямокутник 1×1 і всю таблицю.',
      'Порахуй O(nm) пам’яті в байтах до створення vector<vector<long long>>.',
    ),
  ],
  'parity-remainders': [
    {
      title: 'Класи остач і циклічні стани',
      blocks: [
        paragraph(
          'Остача x mod m показує не саме число, а його клас серед m можливих станів. Додавання і множення можна виконувати над цими класами: (a + b) mod m та (a · b) mod m. Саме тому великі числа часто не потрібно зберігати повністю, якщо відповідь залежить лише від подільності або позиції в циклі.',
        ),
        table(
          ['Властивість', 'Корисний стан'],
          [
            ['парність суми', 'кількість непарних mod 2'],
            ['подільність на m', 'сума mod m'],
            ['циклічний індекс', '(position + shift) mod m'],
          ],
        ),
        code(
          `int mod(long long x, int m) {
    return int((x % m + m) % m);
}

int next = mod(position + shift, cycleLength);`,
          'Нормалізація від’ємної остачі в C++',
        ),
        note(
          'C++ і від’ємні числа',
          'У C++ вираз -1 % 5 дорівнює -1, а не 4. Якщо остача використовується як індекс або стан 0…m−1, нормалізуй її формулою (x % m + m) % m.',
        ),
      ],
    },
    checkpoint(
      'Коли відповідь залежить від остач, спробуй перейти від чисел до частот класів 0…m−1. Наприклад, для пар із сумою, кратною m, клас r поєднується з (m − r) mod m. Окремо обробляються r = 0 та, для парного m, r = m/2.',
      'Доведи, що повне значення числа більше не впливає на відповідь.',
      'Нормалізуй від’ємні остачі.',
      'Виконуй modulo після операцій, якщо числа можуть рости.',
      'Перевір m = 1 і класи, що доповнюють самі себе.',
    ),
  ],
  'gcd-lcm-primes': [
    {
      title: 'Euclid, простота і межі обчислень',
      blocks: [
        paragraph(
          'GCD знаходять алгоритмом Евкліда: gcd(a, b) = gcd(b, a mod b), доки b не стане нулем. У C++ є std::gcd. LCM обчислюють як a / gcd(a, b) * b: ділення перед множенням зменшує ризик overflow. Для перевірки простоти одного числа достатньо перебрати дільники до sqrt(n), тобто поки d * d <= n.',
        ),
        code(
          `long long g = std::gcd(a, b);
long long l = a / g * b;

bool isPrime(long long n) {
    if (n < 2) return false;
    for (long long d = 2; d <= n / d; ++d) {
        if (n % d == 0) return false;
    }
    return true;
}`,
          'GCD, LCM і безпечна перевірка d² ≤ n',
        ),
        paragraph(
          'Якщо потрібно відповісти для багатьох чисел до відомої межі, окрема O(sqrt(n))-перевірка може бути надто дорогою — тоді застосовують решето Ератосфена. Отже, вибір залежить не лише від максимального n, а й від кількості запитів.',
        ),
        note(
          'Факторизація за O(sqrt(n))',
          'Під час перебору дільника d діли n на d у циклі та рахуй степінь. Після циклу залишок n > 1 є останнім простим множником. Це extension до базової перевірки простоти.',
        ),
      ],
    },
    checkpoint(
      'Binary exponentiation обчислює a^p за O(log p): парний степінь ділиться навпіл, а непарний додатково бере один множник a. У модульних задачах множення виконують за модулем після кожного кроку; це не лише прискорює, а й стримує ріст значень.',
      'Розділяй задачу на один запит і багато запитів до однієї межі.',
      'Для циклу d*d <= n використовуй long long або умову d <= n/d.',
      'Обчислюй lcm як a/gcd(a,b)*b.',
      'Перевір n < 2, n = 2, повний квадрат і великий простий залишок.',
    ),
  ],
  'invariants-observations': [
    {
      title: 'Як формулювати і доводити інваріант',
      blocks: [
        paragraph(
          'Інваріант — властивість, яка не змінюється після кожної дозволеної операції. Щоб використати його коректно, потрібно назвати величину, довести її збереження для всіх типів ходів і порівняти початковий та бажаний стани. Різні значення інваріанта доводять неможливість, але однакові значення не завжди гарантують, що перетворення можливе.',
        ),
        list(
          'Перевір парність, суму, XOR, кількість компонентів або значення modulo m.',
          'Розглянь одну операцію і запиши, як саме вона змінює кандидата.',
          'Розділяй необхідну умову («інакше неможливо») і достатню («ось конструкція»).',
          'Для конструктивної задачі перетвори доказ достатності на алгоритм.',
        ),
        code(
          `long long value = 1;
long long power = base;
long long exponent = degree;
while (exponent > 0) {
    if (exponent & 1LL) value = value * power % mod;
    power = power * power % mod;
    exponent >>= 1LL;
}`,
          'Binary exponentiation: степінь за O(log degree)',
        ),
        note(
          'Спостереження треба перетворити на твердження',
          'Кілька вдалих прикладів — ще не доказ. Сформулюй, що саме завжди істинне, перевір контрприклади, а потім доведи необхідність і, якщо потрібно, достатність.',
        ),
      ],
    },
    checkpoint(
      'Інваріант корисний у двох напрямках. Він може довести неможливість: потрібний фінальний стан має інше незмінне значення. Або підтримати конструкцію: кожна операція зберігає допустимість, доки ми не досягнемо цілі. В обох випадках окремо доведи початкову істинність і збереження одним кроком.',
      'Спробуй парність, суму, multiset, кількість компонент або порядок.',
      'Перевір інваріант на кожному дозволеному типі операції.',
      'Не плутай необхідну умову з достатньою.',
      'Для constructive-рішення поясни, чому процес гарантовано завершується.',
    ),
  ],
};
