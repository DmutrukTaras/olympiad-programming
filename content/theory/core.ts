import type { TheorySections } from '@/content/theory/helpers';
import { code, list, note, paragraph, table } from '@/content/theory/helpers';

const checkpoint = (
  explanation: string,
  ...steps: string[]
): TheorySections[number] => ({
  title: 'Самоперевірка моделі',
  blocks: [paragraph(explanation), list(...steps)],
});

export const coreTheoryAdditions: Record<string, TheorySections> = {
  'sorted-two-pointers': [
    {
      title: 'Монотонна область пошуку',
      blocks: [
        paragraph(
          'Два вказівники працюють не просто тому, що масив відсортований. Після кожної перевірки ми маємо вміти безпечно відкинути цілий ряд кандидатів. Для суми пари: якщо a[l] + a[r] замала, то пари (l, j) для j ≤ r теж не кращі — збільшувати потрібно l. Якщо сума завелика, аналогічно зменшуємо r. Це і є доказ коректності руху.',
        ),
        list(
          'Визнач, що означають l і r та яку область кандидатів вони ще не відкинули.',
          'Для кожної гілки доведи, чому один рух не пропускає відповідь.',
          'Зафіксуй умову завершення: l < r для пари різних позицій, l <= r для пошуку одного елемента.',
        ),
        note(
          'Дублікати',
          'Якщо треба порахувати кількість пар, одного руху недостатньо: однакові значення потрібно згрупувати й помножити їх кількості. Якщо потрібні початкові індекси, сортуй пари {value, index}.',
        ),
      ],
    },
    {
      title: 'Шаблон C++ і контроль індексів',
      blocks: [
        code(
          `sort(a.begin(), a.end());
int left = 0;
int right = int(a.size()) - 1;

while (left < right) {
    long long sum = 1LL * a[left] + a[right];
    if (sum == target) break;
    if (sum < target) ++left;
    else --right;
}`,
          'Пара із заданою сумою',
        ),
        paragraph(
          'Індекси мають знаковий тип int, щоб для порожнього масиву right міг стати −1. Вираз int(a.size()) виконує явне перетворення з size_t. Для суми використано long long, бо два int біля межі можуть переповнити int ще до порівняння.',
        ),
      ],
    },
    checkpoint(
      'Намалюй таблицю всіх пар для масиву з 5 елементів. Рух вказівника повинен викреслювати цілий ряд або стовпець кандидатів, і ти маєш уміти пояснити чому. Якщо після руху відкидається лише перевірена пара, прискорення до O(n) не доведене.',
      'Сортування дозволене умовою відповіді або індекси збережені.',
      'Рух кожної межі монотонний і не перевищує n кроків.',
      'Умова для дублікатів сформульована окремо.',
    ),
  ],
  'valid-window': [
    {
      title: 'Чому ліва межа рухається лише вперед',
      blocks: [
        paragraph(
          'Sliding Window потребує монотонної валідності: після додавання правого елемента порушення можна виправити, лише викидаючи елементи зліва. Наприклад, для невід’ємних чисел зменшення відрізка не збільшує суму. Для довільних від’ємних чисел це вже неправда, тому звичайне вікно для обмеження на суму може пропускати відповідь.',
        ),
        table(
          ['Потрібно знайти', 'Коли оновлювати відповідь'],
          [
            ['найдовший валідний відрізок', 'після циклу відновлення валідності'],
            ['найкоротший відрізок, що досягає умови', 'поки умова істинна, перед зсувом left'],
          ],
        ),
        note(
          'while, а не if',
          'Один новий елемент може вимагати викинути кілька лівих. Тому інваріант відновлює цикл while. Після нього вікно знову валідне і готове до наступного right.',
        ),
      ],
    },
    {
      title: 'Універсальний каркас',
      blocks: [
        code(
          `int left = 0;
for (int right = 0; right < n; ++right) {
    add(a[right]);

    while (!valid()) {
        remove(a[left]);
        ++left;
    }

    answer = max(answer, right - left + 1);
}`,
          'Найдовше валідне вікно',
        ),
        paragraph(
          'Функції add/remove мають оновлювати всю локальну інформацію за O(1) або O(log n). Кожен елемент входить у вікно один раз і виходить не більше одного разу, тому два вкладені на вигляд цикли разом виконують O(n) зсувів.',
        ),
      ],
    },
    checkpoint(
      'Перевір predicate не лише на розширення, а й на скорочення. Для стандартного вікна видалення лівих елементів має зрештою відновити валідність. Якщо властивість може хаотично зникати й повертатися через від’ємні внески, потрібні prefix, deque, binary search або інша модель.',
      'Після while вікно гарантовано валідне.',
      'Відповідь оновлюється у правильний момент для longest/shortest.',
      'Порожнє вікно та один елемент не порушують доступ до індексів.',
    ),
  ],
  'frequency-window': [
    {
      title: 'Локальна статистика замість повторного перегляду',
      blocks: [
        paragraph(
          'Для умови на символи або значення у вікні зберігай достатню статистику: частоти, кількість різних елементів, число елементів із завеликою частотою тощо. Ключове слово — достатню. Якщо valid() щоразу проходить по всій map, лінійна ідея легко перетвориться на O(nk). Лічильники потрібно оновлювати інкрементально в add/remove.',
        ),
        code(
          `unordered_map<int, int> freq;
int distinct = 0;

auto add = [&](int x) {
    if (freq[x]++ == 0) ++distinct;
};
auto remove = [&](int x) {
    if (--freq[x] == 0) {
        freq.erase(x);
        --distinct;
    }
};`,
          'Кількість різних значень у поточному вікні',
        ),
        note(
          'Рівно k через «не більше»',
          'Кількість підмасивів рівно з k різними часто рахують як atMost(k) − atMost(k − 1). Умова «не більше k» монотонна для вікна, а «рівно k» безпосередньо — ні.',
        ),
      ],
    },
    {
      title: 'Рядки та фіксований алфавіт',
      blocks: [
        paragraph(
          'Для малих відомих алфавітів array швидший і простіший за map. Наприклад, для латинських малих літер достатньо array<int, 26> і індексу s[i] − ‘a’. Для довільних Unicode-символів така модель вже не підходить: std::string зберігає байти, а не повноцінні символи Unicode.',
        ),
        code(
          `array<int, 26> freq{};
for (int right = 0; right < int(s.size()); ++right) {
    ++freq[s[right] - 'a'];
    // за потреби зсуваємо left і зменшуємо частоту
}`,
          'Частоти для алфавіту a…z',
        ),
      ],
    },
    checkpoint(
      'Сформулюй valid() через один-два лічильники, а не через повний перегляд freq. Під час add/remove відстежуй моменти переходу частоти 0→1, 1→0 або limit→limit+1. Саме ці переходи змінюють кількість різних чи проблемних значень.',
      'Область ключів визначає array, vector або hash map.',
      'remove є точним оберненням add для всієї статистики.',
      'Для exactly k перевір формулу через два atMost.',
    ),
  ],
  'binary-search': [
    {
      title: 'Інваріант меж важливіший за шаблон',
      blocks: [
        paragraph(
          'Binary Search підтримує дві області, про які ми вже щось знаємо, і щоразу вдвічі зменшує невідому частину. Надійний варіант для lower_bound працює з півінтервалом [left, right): відповідь завжди лежить усередині, right може дорівнювати n, а цикл завершується при left == right.',
        ),
        code(
          `int left = 0;
int right = int(a.size());
while (left < right) {
    int mid = left + (right - left) / 2;
    if (a[mid] < x) left = mid + 1;
    else right = mid;
}
// left — перша позиція з a[left] >= x або n`,
          'Власний lower_bound на [left, right)',
        ),
        table(
          ['STL-функція', 'Повертає'],
          [
            ['lower_bound', 'перший елемент >= x'],
            ['upper_bound', 'перший елемент > x'],
            ['binary_search', 'лише true / false'],
          ],
        ),
        note(
          'Передумова STL',
          'lower_bound і upper_bound коректні лише на діапазоні, відсортованому за сумісним порядком. Завжди перевіряй it != a.end() перед розіменуванням.',
        ),
      ],
    },
    {
      title: 'Помилки на межах',
      blocks: [
        list(
          'Не змішуй inclusive [l, r] і half-open [l, r) в одному шаблоні.',
          'Обчислюй mid як left + (right − left) / 2, щоб уникнути overflow.',
          'Перевір масиви розміру 0 і 1, відповідь перед першим та після останнього елемента.',
          'Після циклу інтерпретуй позицію, а не припускай, що потрібний елемент існує.',
        ),
      ],
    },
    checkpoint(
      'Проведи цикл вручну для відповіді на першій позиції, останній позиції та n. На кожному кроці запиши, що гарантовано відомо про область до left і від right. Це швидко виявляє нескінченний цикл та неправильне оновлення mid.',
      'Шаблон використовує одну конвенцію меж від початку до кінця.',
      'Кожна ітерація строго зменшує невідому область.',
      'Результат end() перевіряється до доступу до значення.',
    ),
  ],
  'binary-search-on-answer': [
    {
      title: 'Від оптимізації до монотонного predicate',
      blocks: [
        paragraph(
          'У Binary Search on Answer ми не шукаємо відповідь у масиві. Ми ставимо запитання can(x): «чи існує допустиме рішення з параметром x?». Якщо істинність утворює послідовність false…false, true…true, можна знайти перше true. Для maximize часто зручніше формулювати true…true, false…false і шукати останнє true.',
        ),
        list(
          'Сформулюй x та одиниці вимірювання відповіді.',
          'Доведи монотонність can(x), не покладайся на приклади.',
          'Знайди гарантовано неможливу та гарантовано можливу межі.',
          'Переконайся, що одна перевірка достатньо швидка: загалом O(check · log range).',
        ),
        note(
          'Найпоширеніша логічна помилка',
          'Якщо can(x) істинне, але ти не можеш довести, що can(x + 1) теж істинне (або навпаки для іншого напряму), binary search не має передумови.',
        ),
      ],
    },
    {
      title: 'Шаблон першого true для long long',
      blocks: [
        code(
          `long long left = impossible;
long long right = possible;
while (right - left > 1) {
    long long mid = left + (right - left) / 2;
    if (can(mid)) right = mid;
    else left = mid;
}
cout << right << '\\n';`,
          'left — false, right — true',
        ),
        paragraph(
          'Цей варіант явно зберігає інваріант: left завжди неможливе, right завжди можливе. Для меж біля LLONG_MAX різниця теж може переповнитись, тому практично добирай найтісніші межі з умови або використовуй ширший тип для проміжних обчислень.',
        ),
      ],
    },
    checkpoint(
      'Випиши кілька x навколо очікуваної відповіді й значення can(x). Ряд повинен мати рівно один перехід між false і true. Після цього визнач, який бік зберігає mid у кожній гілці та що означає фінальна межа.',
      'Обидві початкові межі мають доведену істинність.',
      'can(x) не змінює глобальний стан між викликами.',
      'Повна складність включає log діапазону значень, не log n.',
    ),
  ],
  'minimax-search': [
    {
      title: 'Перетворення minimize maximum',
      blocks: [
        paragraph(
          'Замість прямого конструювання оптимального розбиття зафіксуй поріг x і перевір, чи можна зробити всі частини не більшими за x. Зі збільшенням x обмеження слабшає, тому predicate монотонний. Для maximize minimum напрям протилежний: більша мінімальна відстань робить задачу складнішою.',
        ),
        table(
          ['Форма цілі', 'Типове can(x)'],
          [
            ['мінімізувати максимум', 'чи можна з максимумом ≤ x?'],
            ['максимізувати мінімум', 'чи можна з мінімумом ≥ x?'],
          ],
        ),
        paragraph(
          'Перевірка часто greedy, але її теж потрібно довести. Наприклад, якщо елементи невід’ємні, заповнення поточної групи максимально довго використовує не більше груп, ніж будь-яке раннє розділення. З від’ємними значеннями цей аргумент може зламатися.',
        ),
        note(
          'Відповідь і конструкція',
          'Binary search знаходить оптимальне значення. Якщо потрібно вивести сам розподіл, повторно запусти can(answer), цього разу зберігаючи зроблені вибори. Не ускладнюй кожну перевірку реконструкцією.',
        ),
      ],
    },
    {
      title: 'Межі пошуку',
      blocks: [
        code(
          `long long low = *max_element(a.begin(), a.end()) - 1;
long long high = accumulate(a.begin(), a.end(), 0LL);
// low неможливе, high можливе для розбиття невід’ємного масиву
while (high - low > 1) {
    long long mid = low + (high - low) / 2;
    if (can(mid)) high = mid;
    else low = mid;
}`,
          'Minimize maximum sum of a group',
        ),
      ],
    },
    checkpoint(
      'Розрізняй оптимальне значення і допустиму конструкцію. Predicate має відповідати лише «можна чи ні» для заданого порога, тому часто він простіший за початкову задачу. Але якщо greedy-check не доведений, binary search лише багато разів викликає неправильну функцію.',
      'Ціль переписана як нерівність із порогом x.',
      'Напрям монотонності узгоджений з minimize/maximize.',
      'Після пошуку конструкція відновлюється окремим запуском.',
    ),
  ],
  'sorting-greedy': [
    {
      title: 'Як доводити greedy після сортування',
      blocks: [
        paragraph(
          'Greedy-рішення складається з двох тверджень: обраний порядок дозволяє робити локальний вибір, а сам вибір можна включити до деякого оптимального розв’язку. Типовий exchange argument бере оптимальний розв’язок, який не починається з нашого елемента, міняє елементи місцями та доводить, що якість не погіршилась.',
        ),
        list(
          'Назви локальний вибір одним реченням.',
          'Поясни, чому сортувальний ключ відображає дефіцитний ресурс: дедлайн, вартість, кінець інтервалу.',
          'Спробуй знайти контрприклад на 3–5 елементах.',
          'Оформи доказ через обмін, stay-ahead або незворотну властивість.',
        ),
        note(
          'Схожість не є доказом',
          'Те, що у відомій задачі сортували за зростанням, не означає, що цей порядок правильний тут. Навіть невелика зміна цілі може вимагати іншого ключа або DP.',
        ),
      ],
    },
    {
      title: 'Реалізація без прихованих припущень',
      blocks: [
        code(
          `sort(items.begin(), items.end(), [](const Item& a, const Item& b) {
    if (a.primary != b.primary) return a.primary < b.primary;
    return a.secondary < b.secondary;
});

long long answer = 0;
for (const Item& item : items) {
    if (canTake(item)) take(item, answer);
}`,
          'Порядок → один прохід → локальний вибір',
        ),
        paragraph(
          'У доказі та comparator має бути той самий порядок. Tie-breaker іноді не впливає на відповідь, але це теж варто обґрунтувати; якщо впливає, він є частиною алгоритму, а не косметикою.',
        ),
      ],
    },
    checkpoint(
      'Порівняй greedy з усіма варіантами на малому input і знайди перше місце, де оптимальний порядок відрізняється. Exchange argument повинен дозволити виправити саме це місце, не погіршивши відповідь і не зламавши наступні вибори.',
      'Локальний вибір і сортувальний ключ сформульовані окремо.',
      'Доказ охоплює рівні ключі та tie-breaker.',
      'Контрприклади перевірені не лише випадково, а й на крайніх структурах.',
    ),
  ],
  'interval-greedy': [
    {
      title: 'Спочатку уточни модель інтервалів',
      blocks: [
        paragraph(
          'Інтервали [l, r], [l, r) та відкриті межі мають різні правила сумісності. Якщо один закінчується в момент t, а інший починається в t, чи можна взяти обидва? Відповідь визначає перевірку next.l >= lastEnd або next.l > lastEnd. Такі деталі повинні бути узгоджені до написання greedy.',
        ),
        table(
          ['Ціль', 'Типовий порядок'],
          [
            ['максимум неперетинних інтервалів', 'за правим кінцем ↑'],
            ['об’єднати покриття', 'за лівим кінцем ↑'],
            ['мінімум точок, що покривають інтервали', 'за правим кінцем ↑'],
          ],
        ),
        paragraph(
          'Для activity selection найраніше завершення залишає не менше місця всім наступним інтервалам. Exchange argument замінює перший інтервал оптимального розв’язку на наш: правий край не збільшується, отже решта виборів лишається допустимою.',
        ),
        note(
          'Не всі interval-задачі greedy',
          'Якщо інтервали мають ваги й треба максимізувати суму ваг, вибір найранішого завершення вже може бути неправильним. Класичний наступний кандидат — Weighted Interval Scheduling DP.',
        ),
      ],
    },
    {
      title: 'Компактний C++-шаблон',
      blocks: [
        code(
          `sort(intervals.begin(), intervals.end(), [](auto a, auto b) {
    if (a.second != b.second) return a.second < b.second;
    return a.first < b.first;
});

int chosen = 0;
int lastEnd = numeric_limits<int>::min();
for (auto [left, right] : intervals) {
    if (left >= lastEnd) {
        ++chosen;
        lastEnd = right;
    }
}`,
          'Максимум неперетинних півінтервалів',
        ),
      ],
    },
    checkpoint(
      'Намалюй інтервали, які лише торкаються, повністю вкладені та мають однаковий правий край. Ці три випадки перевіряють модель меж, comparator і правило вибору. Після цього сформулюй exchange: заміна на раніший кінець не зменшує простір для решти.',
      'Відкритість меж узгоджена з оператором порівняння.',
      'Ціль справді про кількість, а не суму ваг.',
      'Порожня відповідь і від’ємні координати підтримуються.',
    ),
  ],
  'constructive-algorithms': [
    {
      title: 'Не шукати відповідь, а побудувати її',
      blocks: [
        paragraph(
          'У constructive-задачі правильних відповідей може бути багато. Спочатку виведи необхідні умови: парність, сума, кількість елементів, графові степені. Потім знайди простий будівельний блок, який гарантовано зберігає ці умови, і повторюй його. Часто симетрія, чергування або групування пар прибирають складний глобальний контроль.',
        ),
        list(
          'Перевір, коли відповідь точно неможлива.',
          'Побудуй найменший нетривіальний приклад вручну.',
          'Знайди блок, який можна додавати без порушення інваріанта.',
          'Після побудови програмно перевір усі вимоги — це дешевий страховий assert під час розробки.',
        ),
        note(
          'Достатність потрібна окремо',
          'Необхідна умова лише відсіює неможливі inputs. Щоб оголосити її достатньою, треба дати конструкцію для кожного input, який цю умову виконує.',
        ),
      ],
    },
    {
      title: 'Зручні інструменти C++',
      blocks: [
        code(
          `vector<int> answer;
answer.reserve(n);

for (int value = 1; value <= n; value += 2) {
    if (value + 1 <= n) {
        answer.push_back(value + 1);
        answer.push_back(value);
    }
}

for (int x : answer) cout << x << ' ';`,
          'Побудова відповіді блоками',
        ),
        paragraph(
          'vector дозволяє спочатку зібрати конструкцію, перевірити її розмір та властивості, і лише потім вивести. reserve не змінює size(), але заздалегідь виділяє пам’ять і зменшує кількість перевиділень.',
        ),
      ],
    },
    checkpoint(
      'Після побудови напиши окремий checker для малих тестів: перевір розмір, допустимість кожного елемента, унікальність і глобальну вимогу. Checker не входить у фінальний submit, але швидко знаходить випадки, які словесна конструкція пропустила.',
      'Неможливість доведена, а не вгадана за прикладами.',
      'Кожен будівельний блок зберігає потрібний інваріант.',
      'Кількість побудованих елементів точно збігається з вимогою.',
    ),
  ],
  'stack-queue-deque': [
    {
      title: 'Порядок доступу визначає структуру',
      blocks: [
        paragraph(
          'Stack, queue і deque відрізняються не способом зберігання, а дозволеним порядком операцій. Stack — LIFO: останній доданий виходить першим. Queue — FIFO: перший доданий виходить першим. Deque підтримує обидва кінці. Вибір структури часто прямо випливає з того, який елемент стає доступним наступним.',
        ),
        table(
          ['Структура', 'Додати', 'Подивитися / видалити'],
          [
            ['stack', 'push', 'top / pop'],
            ['queue', 'push', 'front / pop'],
            ['deque', 'push_front/back', 'front/back, pop_front/back'],
          ],
        ),
        note(
          'pop нічого не повертає',
          'У STL спочатку прочитай top(), front() чи back(), а потім викликай pop(). Перед доступом перевір empty(), якщо структура може бути порожньою.',
        ),
      ],
    },
    {
      title: 'Приклад: правильна вкладеність дужок',
      blocks: [
        code(
          `stack<char> opened;
for (char c : s) {
    if (c == '(') opened.push(c);
    else if (c == ')') {
        if (opened.empty()) return false;
        opened.pop();
    }
}
return opened.empty();`,
          'Stack зберігає ще не закриті дужки',
        ),
        paragraph(
          'Інваріант після кожного символу: stack містить рівно ті відкриті дужки з уже прочитаного prefix, для яких ще немає пари. Для кількох типів дужок у stack зберігають сам символ і перевіряють відповідність пар.',
        ),
      ],
    },
    checkpoint(
      'Опиши словами, який саме елемент повинен оброблятися наступним. «Найновіший незавершений» веде до stack, «найстаріший доступний» — до queue, а потреба керувати обома краями — до deque. Після цього перевір, чи структура ніколи не читається порожньою.',
      'Інваріант пояснює зміст усіх елементів у контейнері.',
      'top/front читається до pop.',
      'Кожна команда має визначену поведінку на порожньому стані.',
    ),
  ],
  'monotonic-stack': [
    {
      title: 'Домінування кандидатів',
      blocks: [
        paragraph(
          'Монотонний stack зберігає лише елементи, які ще можуть стати відповіддю для майбутніх позицій. Коли новий елемент робить верхівку безнадійною, її видаляють. Вона більше ніколи не повернеться, тому загальна кількість push і pop лінійна, хоча всередині for стоїть while.',
        ),
        code(
          `vector<int> previousSmaller(n, -1);
stack<int> st; // індекси, значення строго зростають
for (int i = 0; i < n; ++i) {
    while (!st.empty() && a[st.top()] >= a[i]) st.pop();
    if (!st.empty()) previousSmaller[i] = st.top();
    st.push(i);
}`,
          'Найближчий строго менший зліва',
        ),
        note(
          'Строге чи нестроге порівняння',
          'Оператор > або >= визначає поведінку дублікатів. Спершу сформулюй, чи потрібен «менший» або «менший чи рівний», і лише тоді обирай умову pop.',
        ),
      ],
    },
    {
      title: 'Чотири напрямки однієї ідеї',
      blocks: [
        paragraph(
          'Nearest greater/smaller зліва отримуємо проходом зліва направо; справа — проходом у зворотному напрямку. Зміна монотонності stack та оператора порівняння дає чотири базові варіанти. Зручно спочатку написати словами, що повинно лишатися у stack після while.',
        ),
        list(
          'Зберігай індекси, якщо потрібна відстань або межа.',
          'Кожен індекс додається і видаляється не більше одного разу: O(n).',
          'Не плутай відповідь для поточного елемента з моментом, коли відповідь знаходиться під час pop.',
        ),
      ],
    },
    checkpoint(
      'Для кожного pop назви свідка, який домінує видалений елемент. Новий кандидат має бути не гіршим для всіх майбутніх позицій; інакше видалення небезпечне. Потім окремо виріши долю рівних значень — саме там змінюється > на >=.',
      'У stack лежать індекси, якщо потрібні відстані.',
      'Монотонність після push відновлена.',
      'Кожен індекс входить і виходить не більше одного разу.',
    ),
  ],
  'monotonic-queue': [
    {
      title: 'Дві різні причини видалення',
      blocks: [
        paragraph(
          'У monotonic deque індекси видаляються зліва, коли вийшли за межі вікна, і справа, коли новий елемент їх домінує. Для максимуму значення у deque спадають: якщо a[new] ≥ a[back], старий back не стане максимумом раніше за new, бо він і менший, і покине вікно раніше.',
        ),
        code(
          `deque<int> dq;
for (int right = 0; right < n; ++right) {
    while (!dq.empty() && dq.front() <= right - k) dq.pop_front();
    while (!dq.empty() && a[dq.back()] <= a[right]) dq.pop_back();
    dq.push_back(right);

    if (right + 1 >= k) cout << a[dq.front()] << ' ';
}`,
          'Максимум кожного вікна довжини k',
        ),
        note(
          'Чому зберігаємо індекси',
          'Значення достатньо для порівняння, але лише індекс показує, чи елемент уже вийшов із вікна. Через це deque майже завжди містить позиції, а значення беруть як a[dq.front()].',
        ),
      ],
    },
    {
      title: 'Амортизована складність',
      blocks: [
        paragraph(
          'Окремий крок може видалити багато елементів, але кожен індекс потрапляє у deque один раз і видаляється не більше одного разу з кожного боку. Тому сумарно маємо O(n), а не O(nk). Це приклад амортизованого аналізу: оцінюємо всю послідовність операцій, а не найгірший один крок.',
        ),
        table(
          ['Потрібно', 'Монотонність deque'],
          [
            ['максимум у вікні', 'значення спадають'],
            ['мінімум у вікні', 'значення зростають'],
          ],
        ),
      ],
    },
    checkpoint(
      'У monotonic deque є дві незалежні причини видалення: front вийшов за ліву межу вікна, back став гіршим за новий елемент. Не змішуй їх в одну умову. Після обох кроків front повинен бути відповіддю саме для поточного вікна.',
      'Deque зберігає індекси в допустимому діапазоні.',
      'Значення монотонні в потрібному напрямку.',
      'Дублікати обробляються відповідно до вимоги про найраніший/найпізніший індекс.',
    ),
  ],
};
