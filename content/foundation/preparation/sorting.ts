import { code, note, p, table } from '@/content/foundation/helpers';
import type { PatternPreparation } from '@/types/content';

export const sortingPreparation: Record<string, PatternPreparation> = {
  'sorting-to-simplify': {
    introduction:
      'Сортування — не лише спосіб красиво вивести числа. Воно створює порядок, у якому легше помічати сусідів, групи однакових значень і межі. Перед задачами розберімося, що саме змінює sort і як передати йому потрібний діапазон.',
    sections: [
      {
        title: 'Яку інформацію дає порядок',
        blocks: [
          p(
            'Після сортування за неспаданням виконується a[i] <= a[i + 1]. Найменший елемент опиняється на початку, найбільший — у кінці, а рівні значення утворюють суцільні групи. Це може замінити складний пошук одним проходом.',
          ),
          table(
            ['Представлення', '0', '1', '2', '3', '4', '5'],
            [
              ['До сортування', '7', '2', '7', '1', '4', '2'],
              ['Після сортування', '1', '2', '2', '4', '7', '7'],
            ],
          ),
          p(
            'Водночас початкові позиції губляться. Якщо задача залежить від порядку подій, не можна просто відсортувати дані й забути про час. Якщо потрібні початкові індекси, зберігай їх разом зі значеннями.',
          ),
        ],
      },
      {
        title: 'sort, begin та end',
        blocks: [
          p(
            'Функція sort із <algorithm> змінює переданий контейнер на місці й нічого не повертає. begin() та end() — ітератори: вони позначають позиції в контейнері. Діапазон [begin, end) включає початок, але не включає кінець. end() стоїть уже ПІСЛЯ останнього елемента.',
          ),
          code(
            `vector<int> a = {7, 2, 7, 1, 4, 2}; // <vector>
sort(a.begin(), a.end());
for (int x : a) cout << x << ' ';   // 1 2 2 4 7 7
cout << '\\n';

sort(a.rbegin(), a.rend());         // зворотні ітератори
for (int x : a) cout << x << ' ';   // 7 7 4 2 2 1`,
            'За неспаданням та за незростанням',
          ),
          code(
            `vector<int> a = {9, 4, 1, 3, 8};
sort(a.begin() + 1, a.begin() + 4);
// Сортуємо індекси 1, 2, 3. Отримаємо {9, 1, 3, 4, 8}.`,
            'Сортування частини: права межа не входить',
          ),
          note(
            'Ітератор — не значення',
            'Не розіменовуй end(): там немає елемента. Запис a = sort(...) теж неправильний, бо sort повертає void. Щоб зберегти оригінал, спочатку створи копію vector<int> b = a; і сортуй b.',
          ),
        ],
      },
      {
        title: 'Приклад: прибрати повтори',
        blocks: [
          p(
            'Функція unique із <algorithm> залишає по одному елементу з кожної послідовної групи рівних значень. Сам розмір vector вона не зменшує: повертає ітератор нового логічного кінця. Зайву частину фізично прибирає erase.',
          ),
          code(
            `vector<int> a = {7, 2, 7, 1, 4, 2};
sort(a.begin(), a.end());          // {1, 2, 2, 4, 7, 7}
auto newEnd = unique(a.begin(), a.end());
a.erase(newEnd, a.end());
for (int x : a) cout << x << ' ';  // 1 2 4 7`,
            'sort → unique → erase',
          ),
          p(
            'auto просить компілятор визначити тип змінної з правої частини; тут це тип ітератора vector. Без сортування unique прибирає лише сусідні дублікати, тому для [7, 2, 7] обидві сімки залишаться.',
          ),
        ],
      },
      {
        title: 'Ціна сортування та перевірка передумов',
        blocks: [
          p(
            'Для n елементів sort виконує O(n log n) порівнянь. Подальший лінійний прохід не змінює загальну оцінку: O(n log n + n) = O(n log n). Якщо порівнюєш не числа, а довгі рядки, одне порівняння вже не обов’язково O(1).',
          ),
          note(
            'Не сортуй знову без причини',
            'Сортування всього масиву на кожному кроці зовнішнього циклу може коштувати O(n² log n). Якщо дані незмінні, зазвичай достатньо відсортувати один раз. А якщо початковий порядок має зміст, спочатку поясни, чому його можна змінити.',
          ),
        ],
      },
    ],
    questions: [
      {
        question: 'Які позиції сортує sort(a.begin() + 2, a.begin() + 5)?',
        answer:
          'Індекси 2, 3 і 4. Права межа 5 не входить. Ітератори мають бути допустимими, тож у vector повинно бути щонайменше 5 елементів.',
      },
      {
        question: 'Чому після unique часто викликають erase?',
        answer:
          'unique лише пересуває елементи й повертає новий логічний кінець. Розмір vector лишається старим. erase(newEnd, a.end()) прибирає непотрібний хвіст.',
      },
    ],
  },
  'custom-sort-order': {
    introduction:
      'Коли об’єкт має кілька полів, «відсортувати» ще не означає однозначного порядку. Потрібно записати, яке поле головне, що робити при рівності та як передати це правило функції sort.',
    sections: [
      {
        title: 'pair і struct: зберігаємо пов’язані поля',
        blocks: [
          p(
            'pair із <utility> поєднує два значення, доступні як .first і .second. Для більшої кількості полів або зрозуміліших назв зручний власний struct. Одне поле не можна сортувати окремо від інших, інакше зв’язок між ними зникне.',
          ),
          code(
            `pair<int, int> item = {50, 3};
cout << item.first << ' ' << item.second << '\\n'; // 50 3

struct Job {
    int id;
    int priority;
    int duration;
};
Job job{4, 8, 15};
cout << job.priority << '\\n'; // 8`,
            'Пара значень та об’єкт з іменованими полями',
          ),
          p(
            'Стандартне порівняння pair є лексикографічним: спочатку first, а при рівності — second. Наприклад, (2, 9) < (3, 1), а (2, 4) < (2, 9). Це зручно для «значення + початковий індекс».',
          ),
        ],
      },
      {
        title: 'Comparator відповідає на одне питання',
        blocks: [
          p(
            'Comparator для sort(a.begin(), a.end(), cmp) повертає true, якщо перший об’єкт має стояти РАНІШЕ за другий. Для завдань з пріоритетом нехай спочатку йде більший priority, а при рівності — менша duration.',
          ),
          code(
            `vector<Job> jobs = {{1, 2, 5}, {2, 4, 8}, {3, 4, 3}};
sort(jobs.begin(), jobs.end(), [](const Job& a, const Job& b) {
    if (a.priority != b.priority) {
        return a.priority > b.priority;
    }
    return a.duration < b.duration;
});
for (const Job& job : jobs) cout << job.id << ' '; // 3 2 1`,
            'Потрібні <vector>, <algorithm>; struct Job із прикладу вище',
          ),
          table(
            ['id', 'priority', 'duration', 'Місце після sort'],
            [
              ['1', '2', '5', '3 — нижчий пріоритет'],
              ['2', '4', '8', '2 — довше за id 3'],
              ['3', '4', '3', '1 — той самий пріоритет, коротше'],
            ],
          ),
          p(
            '[] починає lambda — коротку функцію прямо в місці використання. Порожні дужки [] означають, що вона не захоплює зовнішні змінні. const Job& передає об’єкт без копіювання й забороняє його змінювати. return завершує порівняння та повертає результат.',
          ),
        ],
      },
      {
        title: 'Строгість і рівність',
        blocks: [
          p(
            'Правило має бути узгодженим: об’єкт не може стояти раніше за самого себе; якщо a раніше b, то b не раніше a; якщо a раніше b, а b раніше c, то a раніше c. Для повністю рівних ключів comparator повертає false в обох напрямках.',
          ),
          code(
            `// Неправильно: рівні значення теж дають true.
// return a.duration <= b.duration;

// Правильно: строгий порядок.
// return a.duration < b.duration;`,
            'Для sort використовуй < або >, а не <= чи >=',
          ),
          p(
            'Не замінюй послідовну перевірку полів виразом a.priority > b.priority || a.duration < b.duration. Так коротша робота може «перемогти» навіть із нижчим пріоритетом, і порівняння стане суперечливим.',
          ),
          note(
            'Чи важливий порядок рівних об’єктів?',
            'sort не гарантує збереження початкового порядку елементів з рівними ключами. Якщо він потрібен, використовуй stable_sort із <algorithm>. Або додай початковий індекс як останній ключ, якщо це відповідає умові.',
          ),
        ],
      },
    ],
    questions: [
      {
        question: 'Що повинен повернути cmp(a, a)?',
        answer:
          'false. Comparator задає строгий порядок: жоден об’єкт не має стояти раніше за самого себе. Використання <= часто порушує саме цю вимогу.',
      },
      {
        question: 'Що перевіряємо раніше: тривалість чи пріоритет?',
        answer:
          'Те поле, яке умова оголосила головним. У нашому прикладі — пріоритет. Тривалість порівнюємо тільки тоді, коли пріоритети рівні.',
      },
    ],
  },
  'basic-stl-structures': {
    introduction:
      'Контейнер варто обирати за операціями, а не за знайомою назвою. Розділимо зберігання унікальних значень, частот і черги робіт. Нижче — потрібний мінімум STL з прикладами C++17.',
    sections: [
      {
        title: 'Короткий refresher замість повтору глави 05',
        blocks: [
          p(
            'Основний матеріал цього патерну — таблиця вибору за операціями вище. Повний синтаксис vector, set, map, queue та priority_queue уже розібрано у вступній главі 05 «C++ та STL». Тут достатньо пригадати, що саме повертає потрібна операція.',
          ),
          code(
            `set<int> seen;                // <set>
seen.insert(7);
bool exists = seen.find(7) != seen.end();

map<int, int> freq;            // <map>
++freq[7];                    // ключ → кількість

priority_queue<int> pending;  // <queue>
pending.push(7);
int largest = pending.top();  // прочитати, не видалити`,
            'Один приклад на кожну потрібну роль',
          ),
          note(
            'Наявність і значення — різні операції',
            'find повертає ітератор або end(), а не bool. Читання map[key] створює відсутній ключ із початковим значенням, тому для перевірки наявності використовуй find. У C++17 немає contains.',
          ),
        ],
      },
      {
        title: 'Ціна операцій і типові пастки',
        blocks: [
          table(
            ['Операція', 'Час', 'Передумова'],
            [
              [
                'map / set: пошук, вставка',
                'O(log n)',
                'Ціна порівняння ключів теж має значення',
              ],
              [
                'priority_queue: top / pop',
                'O(1) / O(log n)',
                'Контейнер непорожній',
              ],
              [
                'vector: a[i]',
                'O(1)',
                'Індекс у межах; це не пошук за значенням',
              ],
            ],
          ),
          p(
            'Нехай є q команд і максимум k активних елементів. Заміна O(k)-пошуку на O(log k) робить загальний час O(q log k), але не змінює самі правила задачі. Якщо знадобиться видаляти довільний елемент за id, звичайна priority_queue вже не відповідає всьому набору операцій.',
          ),
          code(
            `if (!pending.empty()) {
    int value = pending.top();
    pending.pop(); // повертає void; значення вже зберегли
    cout << value;
}`,
            'Продовження прикладу з pending вище',
          ),
          note(
            'Не обирай контейнер за однією командою',
            'Перевір увесь набір операцій і правила рівності. set не зберігає дублікати, priority_queue не дає довільного індексованого доступу, а vector не забезпечує швидкого пошуку значення сам по собі.',
          ),
        ],
      },
    ],
    questions: [
      {
        question:
          'Потрібно і перевіряти наявність числа, і знати кількість його появ. Чи достатньо set?',
        answer:
          'Ні: set зберігає лише факт наявності, дублікати не враховує. Використай map<значення, кількість> або масив частот, якщо діапазон чисел малий.',
      },
      {
        question:
          'Після push(3), push(8), push(5) який елемент видадуть queue та звичайна priority_queue?',
        answer:
          'queue.front() — 3, бо він надійшов першим. priority_queue.top() — 8, бо це найбільше значення. Жодна з цих операцій сама не видаляє елемент.',
      },
    ],
  },
};
