import { code, note, p, table } from '@/content/foundation/helpers';
import type { PatternPreparation } from '@/types/content';

export const preprocessingPreparation: Record<string, PatternPreparation> = {
  'prefix-sum': {
    introduction:
      'Preprocessing — це робота наперед, яка здешевлює майбутні запити. Префікс зберігає підсумок від початку послідовності. Спершу побудуємо його вручну, домовимося про індекси й розберемо інструменти C++ для сум та підрахунків.',
    sections: [
      {
        title: 'Навіщо рахувати наперед',
        blocks: [
          p(
            'Для одного запиту суми можна пройти потрібний відрізок. Але q таких запитів у найгіршому випадку коштують O(nq). Якщо масив не змінюється, ми можемо один раз підсумувати його початки, а потім відповідати за O(1). Загальний час буде O(n + q), додаткова пам’ять — O(n).',
          ),
          p(
            'Використаємо зручну домовленість: a має індекси 0 … n − 1, а pref[i] зберігає суму ПЕРШИХ i елементів. pref[0] = 0 — сума порожнього початку. Для n елементів потрібно n + 1 префіксне значення.',
          ),
          table(
            ['i — скільки елементів узяли', '0', '1', '2', '3', '4', '5'],
            [
              ['Щойно додали', '—', '3', '−2', '5', '1', '4'],
              ['pref[i]', '0', '3', '1', '6', '7', '11'],
            ],
          ),
          p(
            'Наприклад, сума a[1] … a[3] дорівнює −2 + 5 + 1 = 4. Беремо перші 4 елементи й віднімаємо перший: pref[4] − pref[1] = 7 − 3. Від’ємні значення не заважають — використовуємо властивості додавання, а не монотонність.',
          ),
        ],
      },
      {
        title: 'Побудова й межі запиту в C++',
        blocks: [
          code(
            `vector<int> a = {3, -2, 5, 1, 4}; // <vector>
int n = static_cast<int>(a.size());
vector<long long> pref(n + 1, 0);
for (int i = 0; i < n; ++i) {
    pref[i + 1] = pref[i] + a[i];
}
int l = 1, r = 3;                // включні індекси від 0
cout << pref[r + 1] - pref[l] << '\\n'; // 4`,
            'pref[i] — сума перших i елементів',
          ),
          table(
            ['Як задано відрізок', 'Формула для цієї таблиці pref'],
            [
              [
                'Індекси від 0, обидві межі включні [l, r]',
                'pref[r + 1] − pref[l]',
              ],
              [
                'Індекси від 0, права межа не входить [l, r)',
                'pref[r] − pref[l]',
              ],
              [
                'Позиції від 1, обидві межі включні [L, R]',
                'pref[R] − pref[L − 1]',
              ],
            ],
          ),
          note(
            'Не вчи формулу без визначення pref',
            'Помилка на одиницю часто виникає через змішування позицій від 1 та індексів від 0. Спочатку напиши словами, що зберігає pref[i]. Потім перевір один елемент, увесь масив і відрізок, що починається з першого елемента.',
          ),
        ],
      },
      {
        title: 'Prefix Count: умова перетворюється на 0 або 1',
        blocks: [
          p(
            'Щоб рахувати кількість підходящих елементів, заміни кожен елемент індикатором: 1, якщо умова виконується, 0 — інакше. У C++ результат порівняння має тип bool; у числовому виразі true стає 1, false — 0.',
          ),
          code(
            `vector<int> a = {3, -2, 5, 1, 4};
int n = static_cast<int>(a.size());
vector<int> negative(n + 1, 0);
for (int i = 0; i < n; ++i) {
    negative[i + 1] = negative[i] + (a[i] < 0);
}
cout << negative[4] - negative[1] << '\\n'; // 1: у a[1..3]`,
            'Кількість від’ємних на відрізку',
          ),
          p(
            'Ми не придумали новий алгоритм: це префіксна сума масиву з нулів та одиниць. Аналогічно можна порахувати нулі, певну літеру або числа, що діляться на k.',
          ),
        ],
      },
      {
        title: 'Числові типи та межі методу',
        blocks: [
          p(
            'Кожен елемент може вміщатися в int, а їхня сума — ні. Наприклад, 200000 чисел по 10⁹ дають 2·10¹⁴. Для таких префіксів потрібен long long. Тип результату зліва не змінює тип уже виконаного множення справа.',
          ),
          code(
            `int n = 200000;
int value = 1000000000;
long long bound = 1LL * n * value; // 200000000000000
// long long bound = n * value;    // неправильно: int переповниться`,
            '1LL переводить обчислення в long long до множення',
          ),
          note(
            'Дані змінилися — префікси застаріли',
            'Після зміни одного a[i] усі наступні префікси можуть змінитися. Для частих оновлень і запитів потрібен інший інструмент, наприклад Fenwick Tree. А для мінімуму не працює «відняти два префіксні мінімуми»: мінімум не можна скасувати відніманням.',
          ),
        ],
      },
    ],
    questions: [
      {
        question: 'Чому для n елементів ми зберігаємо n + 1 префікс?',
        answer:
          'Є n непорожніх початків і ще порожній початок із сумою 0. Він дозволяє однаково обробляти відрізки від першого елемента, не звертаючись до індексу −1.',
      },
      {
        question:
          'Як порахувати кількість чисел, більших за 10, на багатьох відрізках?',
        answer:
          'Побудувати префіксні суми індикаторів (a[i] > 10). Різниця двох таких префіксів дасть кількість підходящих чисел у потрібному відрізку.',
      },
    ],
  },
  'difference-array': {
    introduction:
      'Масив різниць зберігає не самі значення, а те, наскільки вони змінюються між сусідніми позиціями. Це зручно, коли треба багато разів додати число до цілого відрізка, а остаточний масив потрібен лише після всіх змін.',
    sections: [
      {
        title: 'Значення та зміни — два представлення',
        blocks: [
          p(
            'Використаємо позиції від 1 та уявний a[0] = 0. Тоді d[i] = a[i] − a[i − 1]. Щоб відновити a[i], достатньо скласти d[1] … d[i]: проміжні доданки взаємно скорочуються. Тобто префіксна сума різниць повертає початковий масив.',
          ),
          table(
            ['Позиція i', '1', '2', '3', '4', '5'],
            [
              ['a[i]', '2', '2', '5', '5', '1'],
              ['d[i]', '2', '0', '3', '0', '−4'],
              ['Накопичуємо d', '2', '2', '5', '5', '1'],
            ],
          ),
          code(
            `vector<long long> a = {0, 2, 2, 5, 5, 1}; // a[0] — службовий
int n = 5;
vector<long long> diff(n + 2, 0);          // заголовок <vector>
for (int i = 1; i <= n; ++i) {
    diff[i] = a[i] - a[i - 1];
}`,
            'Будуємо різниці вже заповненого масиву',
          ),
        ],
      },
      {
        title: 'Чому достатньо двох позначок',
        blocks: [
          p(
            'Додамо 3 до позицій 2 … 4 у нульовому масиві з п’яти елементів. На позиції 2 має початися дія +3, на позиції 5 — закінчитися. Тому записуємо +3 у diff[2] і −3 у diff[5]. Накопичений підсумок застосовує зміну всередині відрізка й скасовує після нього.',
          ),
          table(
            ['Позиція', '1', '2', '3', '4', '5'],
            [
              ['Позначки diff', '0', '+3', '0', '0', '−3'],
              ['Відновлене значення', '0', '3', '3', '3', '0'],
            ],
          ),
          code(
            `int n = 5;
vector<long long> diff(n + 2, 0);
int l = 2, r = 4;
long long add = 3;
diff[l] += add;
diff[r + 1] -= add;

long long current = 0;
for (int i = 1; i <= n; ++i) {
    current += diff[i];
    cout << current << ' '; // 0 3 3 3 0
}`,
            'Одна зміна відрізка та відновлення значень',
          ),
          p(
            'Кілька додавань просто накладаються: для кожного запиту залишаємо дві позначки, а масив відновлюємо один раз у кінці. Навіть для від’ємного add формула не змінюється. Час — O(q + n), пам’ять — O(n).',
          ),
        ],
      },
      {
        title: 'n + 2, long long і початкові дані',
        blocks: [
          p(
            'За нумерації 1 … n потрібна додаткова комірка n + 1: у ній скасовується оновлення, що доходить до кінця. vector із n + 2 елементів має індекси 0 … n + 1. Саму службову комірку n + 1 не потрібно виводити.',
          ),
          note(
            'Чому +=, а не =',
            'Декілька запитів можуть починатися чи закінчуватися в одній позиції. diff[l] = add зітре попередню позначку, а diff[l] += add додасть новий внесок. І скасовувати його потрібно саме в r + 1, а не в r: права межа входить у відрізок.',
          ),
          p(
            'Якщо масив спочатку ненульовий, є два коректні варіанти: побудувати його різниці й додавати позначки до них або вести окремий нульовий масив змін і в кінці додати накопичену зміну до a[i]. Не змішуй варіанти, інакше початкові значення можуть врахуватися двічі.',
          ),
          note(
            'Коли метод не розв’язує всю задачу',
            'Цей простий варіант відкладає відновлення до кінця. Якщо після кожної зміни потрібна швидка сума або максимум на довільному відрізку, одного такого масиву недостатньо. Також присвоїти x усім елементам — не те саме, що додати x.',
          ),
        ],
      },
    ],
    questions: [
      {
        question:
          'Куди поставити позначки, щоб додати 7 до позицій 3 … 5 включно?',
        answer:
          'diff[3] += 7 та diff[6] -= 7. На позиції 3 зміна починається, а на 6 вже не повинна діяти.',
      },
      {
        question:
          'Чому не варто відновлювати весь масив після кожного запиту, якщо потрібен лише кінцевий результат?',
        answer:
          'Кожне відновлення коштує O(n), і для q запитів знову отримаємо O(nq). Накопичення позначок і один прохід у кінці дають O(n + q).',
      },
    ],
  },
  'prefix-xor-2d': {
    introduction:
      'Ідея префіксів працює не лише для суми в одному вимірі. Для XOR можна скасувати однакові частини, а для матриці — відняти зайві прямокутники. Розберемо ці дві властивості окремо, починаючи з оператора ^ і двовимірного vector.',
    sections: [
      {
        title: 'XOR: бітова операція, не піднесення до степеня',
        blocks: [
          p(
            'XOR порівнює два числа побітово: результат у біті дорівнює 1, якщо біти різні. У C++ це оператор ^. Для невід’ємних чисел корисні властивості x ^ x = 0, x ^ 0 = x, а також незалежність результату від порядку й групування операцій.',
          ),
          table(
            ['Операція', 'У двійковому записі', 'Результат'],
            [
              ['5 ^ 3', '101 XOR 011 = 110', '6'],
              ['5 ^ 5', '101 XOR 101 = 000', '0'],
              ['5 ^ 0', '101 XOR 000 = 101', '5'],
            ],
          ),
          code(
            `vector<int> a = {5, 3, 5, 2}; // <vector>
int n = static_cast<int>(a.size());
vector<int> px(n + 1, 0);
for (int i = 0; i < n; ++i) px[i + 1] = px[i] ^ a[i];

int l = 1, r = 2;            // включні індекси від 0
cout << (px[r + 1] ^ px[l]) << '\\n'; // 3 ^ 5 = 6`,
            'XOR на відрізку: спільний початок скасовується',
          ),
          note(
            'Не плутай оператори',
            '^ — побітовий XOR, не степінь і не логічне «або». || працює з логічними умовами, | — побітове OR. Для XOR-префікса використовуємо XOR двох префіксів, а не їх різницю. Дужки навколо XOR у cout важливі через пріоритет операторів.',
          ),
        ],
      },
      {
        title: 'Матриця у vector<vector<int>>',
        blocks: [
          p(
            'Матриця — таблиця з n рядків та m стовпців. a[row][col] спочатку вибирає рядок, а потім елемент у ньому. Вкладений vector дозволяє створити таку таблицю відомого під час виконання розміру.',
          ),
          code(
            `int n = 2, m = 3;
vector<vector<int>> a(n, vector<int>(m, 0));
for (int row = 0; row < n; ++row) {
    for (int col = 0; col < m; ++col) {
        a[row][col] = row * m + col + 1;
    }
}
cout << a[0][2] << ' ' << a[1][0] << '\\n'; // 3 4`,
            'Матриця [[1, 2, 3], [4, 5, 6]]',
          ),
          p(
            'Тут внутрішній vector<int>(m, 0) описує один рядок із m нулів, а зовнішній створює n незалежних таких рядків. Два вкладені цикли проходять n·m комірок, а не n + m. Перевіряй і час, і пам’ять для їх добутку.',
          ),
        ],
      },
      {
        title: '2D Prefix: врахувати перетин лише один раз',
        blocks: [
          p(
            'Нехай pref[i][j] — сума перших i рядків і перших j стовпців. Нульовий рядок і нульовий стовпець префікса заповнені нулями. Щоб додати нову комірку, беремо верхній прямокутник і лівий, віднімаємо їх спільну частину й додаємо сам елемент.',
          ),
          table(
            ['Матриця a', 'Стовпець 0', 'Стовпець 1', 'Стовпець 2'],
            [
              ['Рядок 0', '1', '2', '3'],
              ['Рядок 1', '4', '5', '6'],
            ],
          ),
          table(
            ['Префікс', 'j = 0', 'j = 1', 'j = 2', 'j = 3'],
            [
              ['i = 0', '0', '0', '0', '0'],
              ['i = 1', '0', '1', '3', '6'],
              ['i = 2', '0', '5', '12', '21'],
            ],
          ),
          code(
            `vector<vector<int>> a = {{1, 2, 3}, {4, 5, 6}};
int n = 2, m = 3;
vector<vector<long long>> pref(n + 1, vector<long long>(m + 1, 0));
for (int i = 1; i <= n; ++i) {
    for (int j = 1; j <= m; ++j) {
        pref[i][j] = pref[i - 1][j] + pref[i][j - 1]
                   - pref[i - 1][j - 1] + a[i - 1][j - 1];
    }
}`,
            'Побудова: O(n·m) часу та пам’яті',
          ),
          p(
            'Для включного прямокутника від (r1, c1) до (r2, c2), де координати від 0, беремо великий префікс, віднімаємо верхню та ліву зайві смуги й повертаємо їх перетин, віднятий двічі.',
          ),
          code(
            `int r1 = 0, c1 = 1, r2 = 1, c2 = 2;
long long sum = pref[r2 + 1][c2 + 1]
              - pref[r1][c2 + 1] - pref[r2 + 1][c1]
              + pref[r1][c1];
cout << sum << '\\n'; // 2 + 3 + 5 + 6 = 16`,
            'Один прямокутний запит: O(1)',
          ),
          note(
            'Дві незалежні осі',
            'Не міняй місцями рядок і стовпець. Для a із нульовими індексами та pref зі службовою рамкою індекси відрізняються на 1. Потрібна саме сума? Використовуй + і −; XOR-префікс із попереднього прикладу — інша операція.',
          ),
        ],
      },
    ],
    questions: [
      {
        question: 'Чому дорівнює 9 ^ 4 ^ 9 і чому?',
        answer:
          '4. Можемо згрупувати однакові дев’ятки: (9 ^ 9) ^ 4 = 0 ^ 4 = 4. Саме так у XOR двох префіксів скасовується спільний початок.',
      },
      {
        question:
          'Чому у формулі суми прямокутника останній префікс додається?',
        answer:
          'Верхню й ліву зайві частини відняли окремо, тому їх перетин відняли двічі. Одне додавання повертає зайве віднімання.',
      },
    ],
  },
};
