import { code, note, p, table } from '@/content/foundation/helpers';
import type { PatternPreparation } from '@/types/content';

export const implementationPreparation: Record<string, PatternPreparation> = {
  'direct-implementation': {
    introduction:
      'Один прохід — це послідовний перегляд даних, під час якого ми зберігаємо лише потрібні підсумки. Перед задачею розберімося з масивом, індексами, циклами та змінними, які пам’ятають результат уже виконаної роботи.',
    sections: [
      {
        title: 'Масив, vector та індекси',
        blocks: [
          p(
            'Масив — впорядкована послідовність елементів. У vector зручно зберігати таку послідовність, коли її довжину дізнаємося під час роботи програми. Потрібен заголовок <vector>. Індекси починаються з 0: у п’яти елементів останній індекс — 4, а не 5.',
          ),
          table(
            ['Індекс i', '0', '1', '2', '3', '4'],
            [['a[i]', '4', '−1', '6', '3', '0']],
          ),
          code(
            `vector<int> a = {4, -1, 6, 3, 0};
cout << a[0] << ' ' << a.size() << '\\n'; // 4 5
a.push_back(8);                          // додаємо в кінець
cout << a.back() << '\\n';               // 8
vector<int> counters(5, 0);              // п’ять нулів`,
            'vector: елемент, розмір і додавання',
          ),
          p(
            'size() повертає кількість елементів. push_back(x) додає елемент, а back() повертає останній. Доступ a[i] — O(1), push_back — амортизовано O(1): окреме розширення може коштувати O(n), але n послідовних додавань загалом коштують O(n).',
          ),
          note(
            'Розмір — не останній індекс',
            'Для vector із n елементами допустимі a[0] … a[n − 1]. Доступ a[n] — вихід за межі. back() не можна викликати для порожнього контейнера. reserve(n) лише резервує пам’ять і не створює n елементів; для цього потрібен vector<int> a(n).',
          ),
        ],
      },
      {
        title: 'string — теж послідовність',
        blocks: [
          p(
            'string із <string> зберігає послідовність символів. Для малих латинських літер один символ займає одну позицію: у "abaca" довжина 5, перший символ має індекс 0, останній — 4. s[i] дає символ, size() — кількість позицій, range-for перебирає символи без індексів.',
          ),
          code(
            `string s = "abaca";
cout << s[0] << ' ' << s.size() << '\\n'; // a 5
int count = 0;
for (char c : s) {
    if (c == 'a') ++count;
}
cout << count << '\\n'; // 3
for (int i = 1; i < static_cast<int>(s.size()); ++i) {
    if (s[i] == s[i - 1]) cout << i << ' ';
} // однакових сусідів тут немає`,
            'Значення символу, позиція та обхід рядка',
          ),
          note(
            'Межі й алфавіт',
            'Перед s[i] перевіряй 0 <= i < s.size(). cin >> s читає слово до пробілу, а getline — рядок. Цей приклад працює з латинськими символами; для українського UTF-8 string індексує байти, не літери.',
          ),
        ],
      },
      {
        title: 'Два способи пройти дані',
        blocks: [
          p(
            'Цикл з індексом потрібен, коли важлива позиція або сусідній елемент. Range-based for зручний, коли достатньо самих значень. Запис int &x означає посилання на елемент: зміна x змінює vector. Без & змінюється лише копія.',
          ),
          code(
            `int n;
cin >> n;
vector<int> a(n);
for (int &x : a) cin >> x;       // записуємо у самі елементи

for (int i = 0; i < n; ++i) {
    cout << i << ':' << a[i] << ' ';
}
for (int x : a) cout << x << ' '; // лише читаємо значення`,
            'Зчитування та обхід vector',
          ),
          p(
            'У for (int i = 0; i < n; ++i) є початкове значення, умова продовження і дія після кожної ітерації. ++i збільшує i на один. Якщо перевіряєш a[i − 1], починай i з 1, а не з 0.',
          ),
          note(
            'Частий непомітний баг',
            'for (int x : a) cin >> x; прочитає ввід, але не запише його у vector. Для вводу використовуй int &x. Межа циклу має бути i < n, а не i <= n.',
          ),
        ],
      },
      {
        title: 'Що зберігати під час проходу',
        blocks: [
          p(
            'Стан алгоритму — змінні, яких достатньо для наступного кроку. Лічильник зберігає кількість, сума — накопичене значення. «Поточний результат» і «найкращий за весь час» — різні ролі: поточний може обнулитися, але найкращий не повинен загубитися.',
          ),
          code(
            `vector<int> a = {4, -1, 6, 3, 0};
int count = 0;
long long sum = 0;
for (int x : a) {
    if (x >= 0 && x % 2 == 0) {
        ++count;
        sum += x;
    }
}
cout << count << ' ' << sum << '\\n'; // 3 10`,
            'Порахуємо кількість і суму невід’ємних парних',
          ),
          table(
            [
              'Прочитали x',
              'Умова виконується?',
              'count після кроку',
              'sum після кроку',
            ],
            [
              ['4', 'так', '1', '4'],
              ['−1', 'ні', '1', '4'],
              ['6', 'так', '2', '10'],
              ['3', 'ні', '2', '10'],
              ['0', 'так', '3', '10'],
            ],
          ),
          p(
            '&& означає «і», || — «або», == перевіряє рівність, а = присвоює значення. x % 2 — остача від ділення на 2. Нуль теж парний, тому останній елемент збільшує count, хоча sum не змінюється. Один прохід має час O(n).',
          ),
          code(
            `int best = -7;
int current = -3;
best = max(best, current); // -3; заголовок <algorithm>`,
            'Оновити найкраще значення',
          ),
          note(
            'Початкове значення має зміст',
            'Лічильник і суму починаємо з 0. Максимум довільних чисел не завжди можна почати з 0: для [−7, −3] це дасть неправильну відповідь. За непорожнього масиву візьми перший елемент. Довгу суму зберігай у long long.',
          ),
        ],
      },
    ],
    questions: [
      {
        question:
          'У vector є 6 елементів. Який останній індекс і що поверне size()?',
        answer:
          'Останній індекс — 5, size() поверне 6. Умова i < 6 перебере всі шість елементів від 0 до 5.',
      },
      {
        question:
          'Чому лічильник підходящих елементів оголошують перед циклом?',
        answer:
          'Він має пам’ятати результат попередніх ітерацій. Якщо створювати int count = 0 усередині циклу, на кожному кроці попередній підсумок губитиметься.',
      },
    ],
  },
  'process-simulation': {
    introduction:
      'Симуляція відтворює правила процесу крок за кроком. Найважливіше — чітко назвати стан, початкові значення і зміни від кожної команди. Це допоможе не загубити деталі умови ще до написання коду.',
    sections: [
      {
        title: 'Стан → команда → новий стан',
        blocks: [
          p(
            'Уявімо термостат: температура спочатку дорівнює 20, команда + збільшує її на 2, команда − зменшує на 2. Дозволені значення — від 16 до 24; команду, яка виходить за межі, ігноруємо. Стан тут — одна змінна temperature. Кількість команд не є станом температури.',
          ),
          table(
            [
              'Крок',
              'Команда',
              'Запропонована температура',
              'Фактична температура',
            ],
            [
              ['0', 'початок', '—', '20'],
              ['1', '+', '22', '22'],
              ['2', '+', '24', '24'],
              ['3', '+', '26 — за межею', '24'],
              ['4', '−', '22', '22'],
            ],
          ),
          p(
            'Таблиця станів — найпростіша перевірка симуляції. Після кожної команди запиши всі змінні, а не тільки відповідь у кінці. Одразу видно, на якому кроці програма розходиться з правилами.',
          ),
        ],
      },
      {
        title: 'Команди в char і string',
        blocks: [
          p(
            'char зберігає один символ, наприклад \'+\'. string — послідовність символів, наприклад "+++-"; для нього потрібен <string>. Одинарні лапки позначають символ, подвійні — рядок. По рядку можна пройтися циклом так само, як по vector.',
          ),
          code(
            `string commands = "+++-";
int temperature = 20;
for (char command : commands) {
    int next = temperature;
    if (command == '+') next += 2;
    else if (command == '-') next -= 2;

    if (16 <= next && next <= 24) {
        temperature = next;
    }
}
cout << temperature << '\\n'; // 22`,
            'Перевіряємо новий стан перед зміною поточного',
          ),
          p(
            'Змінна next дозволяє спочатку перевірити наслідок команди, а тоді прийняти його. else if підкреслює, що команди взаємовиключні. За q команд час O(q); сам стан займає O(1), але збережений рядок команд — O(q) пам’яті.',
          ),
          code(
            `char command;
cin >> command;              // наступний непробільний символ
string word;
cin >> word;                 // слово до пробілу
string line;
getline(cin >> ws, line);    // рядок; початкові пробіли пропускаємо`,
            'Три способи читати текст',
          ),
          note(
            'Різний ввід — різне читання',
            'cin >> word не читає рядок із пробілами повністю. getline читає до кінця рядка. У варіанті cin >> ws початкові пробіли та порожні рядки пропускаються, тому він не підходить, якщо вони є значущими даними.',
          ),
        ],
      },
      {
        title: 'Порядок змін і межі',
        blocks: [
          p(
            'Якщо кілька величин змінюються одночасно, обчислюй нові значення зі старого стану. Наприклад, для одночасного обміну лівого й правого значень не можна спочатку записати left = right, а потім right = left: початкове left уже втрачено.',
          ),
          code(
            `int left = 3, right = 8;
int nextLeft = right;
int nextRight = left;
left = nextLeft;
right = nextRight;
cout << left << ' ' << right << '\\n'; // 8 3
// Сам обмін коротше: swap(left, right); заголовок <utility>.`,
            'Одночасне оновлення через тимчасові змінні',
          ),
          note(
            'Не вгадуй поведінку на межі',
            'Ігнорувати недопустиму команду, обрізати значення до межі й перейти по колу — три різні правила. Їх визначає умова. Також не пиши 16 <= next <= 24: у C++ це не математична подвійна нерівність; потрібне 16 <= next && next <= 24.',
          ),
        ],
      },
    ],
    questions: [
      {
        question:
          'Чому для термостата недостатньо просто порахувати плюси й мінуси?',
        answer:
          'Через обмеження частина команд може ігноруватися, тому важливий порядок. Із температури 20 послідовність +++− завершується на 22, а −+++ — на 24, хоча кількості плюсів і мінусів однакові.',
      },
      {
        question:
          'Чим відрізняються температура після кроку і кількість виконаних команд?',
        answer:
          'Це різні частини стану. Температура описує поточний стан системи; лічильник накопичує події. Якщо умова просить обидва значення, їх потрібно зберігати окремо, чітко визначивши, чи рахується проігнорована команда.',
      },
    ],
  },
  'frequency-counting': {
    introduction:
      'Частота значення — це кількість його появ. Коли порядок елементів неважливий, замість усієї послідовності часто достатньо таблиці частот. Навчимося її будувати й відрізняти номер комірки від кількості всередині.',
    sections: [
      {
        title: 'Значення стає індексом',
        blocks: [
          p(
            'Для даних [2, 0, 2, 3, 2, 0] таблиця freq має значення freq[0] = 2, freq[1] = 0, freq[2] = 3, freq[3] = 1. Тут індекс означає число з вхідних даних, а вміст комірки — кількість його появ. Це вже не початковий масив.',
          ),
          table(
            ['Значення x', '0', '1', '2', '3'],
            [['freq[x]', '2', '0', '3', '1']],
          ),
          code(
            `vector<int> a = {2, 0, 2, 3, 2, 0};
vector<int> freq(4, 0);          // заголовок <vector>
for (int x : a) ++freq[x];

int distinct = 0;
for (int count : freq) {
    if (count > 0) ++distinct;
}
cout << freq[2] << ' ' << distinct << '\\n'; // 3 3`,
            'Частота конкретного числа та кількість різних чисел',
          ),
          p(
            '++freq[x] збільшує одну комірку, а не x. Побудова займає O(n), перегляд усіх можливих значень — O(U), де U — розмір діапазону. Загалом O(n + U) часу та O(U) додаткової пам’яті.',
          ),
        ],
      },
      {
        title: 'Частоти символів у рядку',
        blocks: [
          p(
            "Для малих латинських літер у звичному олімпіадному середовищі можна перевести символ у номер від 0 до 25: c − 'a'. Наприклад, 'a' дає 0, 'b' — 1. Для string потрібен <string>.",
          ),
          code(
            `string s = "banana";
int freq[26]{};                   // усі елементи — нулі
for (char c : s) ++freq[c - 'a'];

cout << freq['a' - 'a'] << '\\n';  // 3
cout << freq['n' - 'a'] << '\\n';  // 2
for (int i = 0; i < 26; ++i) {
    if (freq[i] > 0) {
        cout << char('a' + i) << ':' << freq[i] << ' ';
    }
} // a:3 b:1 n:2`,
            'Підрахунок малих латинських літер',
          ),
          note(
            'Спочатку перевір алфавіт',
            "Формула c − 'a' підходить лише коли ввід містить малі латинські літери. Великі літери, цифри й український текст потребують іншого представлення. Зокрема, string із текстом UTF-8 зберігає байти, а не окремі українські літери.",
          ),
        ],
      },
      {
        title: 'Коли масиву частот достатньо',
        blocks: [
          p(
            'Якщо числа лежать у 0 … 100, можна створити 101 комірку. Для значень від −50 до 50 використай зсув: індекс x + 50. Для довільних чисел до 10⁹ масив такого розміру зазвичай неприйнятний; тоді потрібен map, unordered_map або стискання координат.',
          ),
          table(
            ['Потрібно дізнатися', 'Що перевірити'],
            [
              ['Чи є x?', 'freq[x] > 0'],
              ['Скільки разів є x?', 'freq[x]'],
              ['Чи є повтор x?', 'freq[x] >= 2'],
              ['Скільки різних значень?', 'Кількість комірок із freq[x] > 0'],
            ],
          ),
          note(
            'Нульова частота — не рідкісне наявне значення',
            'Коли шукаєш найрідкісніше серед присутніх значень, пропускай freq[x] == 0. А при рівних частотах окремо виконай правило умови: найменше значення, найбільше чи перше за появою. Таблиця частот сама по собі не зберігає порядок появ.',
          ),
          note(
            'Обнулення обов’язкове',
            'int freq[26]; усередині функції не ініціалізує числа нулями. Використовуй int freq[26]{}; або vector<int> freq(26, 0). Для кількох тестів створи таблицю заново всередині обробки кожного тесту.',
          ),
        ],
      },
    ],
    questions: [
      {
        question:
          'Для [5, 5, 2] чому дорівнюють freq[5] і кількість різних значень?',
        answer:
          'freq[5] = 2, бо п’ятірка трапляється двічі. Різних значень теж два: 2 і 5. Ці величини випадково рівні в цьому прикладі, але означають різне.',
      },
      {
        question:
          'Чи можна відновити початковий порядок елементів лише з таблиці частот?',
        answer:
          'Ні. Наприклад, [1, 2, 1] і [2, 1, 1] мають однакові частоти. Якщо відповідь залежить від позицій або порядку, частот може бути недостатньо.',
      },
    ],
  },
};
